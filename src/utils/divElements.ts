export const playButton: HTMLButtonElement = document.getElementById(
  "play"
) as HTMLButtonElement;

export const resetButton: HTMLButtonElement = document.getElementById(
  "reset"
) as HTMLButtonElement;

export const scoreElement: HTMLDivElement = document.getElementById(
  "score"
) as HTMLDivElement;

export const board: HTMLDivElement = document.getElementById(
  "app"
) as HTMLDivElement;

export const leftButton: HTMLButtonElement = document.getElementById(
  "left"
) as HTMLButtonElement;
export const rightButton: HTMLButtonElement = document.getElementById(
  "right"
) as HTMLButtonElement;
export const rotateButton: HTMLButtonElement = document.getElementById(
  "rotate"
) as HTMLButtonElement;
